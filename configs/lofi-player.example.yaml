# lofi-player configuration.
#
# This file lives at $XDG_CONFIG_HOME/lofi-player/config.yaml
# (typically ~/.config/lofi-player/config.yaml on Linux and macOS).
#
# It is created with these defaults on first run if it doesn't exist.

# Active color theme. Built-ins:
# tokyo-night, catppuccin-mocha, gruvbox-dark, rose-pine,
# catppuccin-latte, rose-pine-dawn, solarized-light, paper.
theme: tokyo-night

# Initial playback volume, 0–100.
volume: 60

# Network buffering for unstable connections.
# buffer_seconds asks mpv to keep this many seconds buffered ahead when the
# stream/server permits it. Try 60–120 on flaky Wi-Fi; 0 leaves mpv defaults.
buffer_seconds: 30
# initial_buffer_seconds waits for this much cache before starting/resuming
# after a cache stall. 5–10 helps bad networks but delays playback start.
initial_buffer_seconds: 0

# Internet-radio stations. Each station needs a display name and a stream URL.
# Share/import uses the same shape, so snippets copied from friends can be
# pasted under this key or imported with `lofi-player --import stations.yaml`.
stations:
  - name: SomaFM Groove Salad
    url: https://ice1.somafm.com/groovesalad-256-mp3
  - name: SomaFM Drone Zone
    url: https://ice1.somafm.com/dronezone-256-mp3
  - name: SomaFM Deep Space One
    url: https://ice1.somafm.com/deepspaceone-128-mp3
  - name: Radio Paradise Mellow
    url: https://stream.radioparadise.com/mellow-128

  # YouTube stations route through mpv's ytdl_hook; you need yt-dlp on
  # $PATH (brew install yt-dlp / pip install yt-dlp). The well-known
  # 24/7 Lofi Girl stream is a good starting point — uncomment to use:
  # - name: Lofi Girl 24/7
  #   url: https://www.youtube.com/watch?v=jfKfPfyJRdk
  #   kind: youtube

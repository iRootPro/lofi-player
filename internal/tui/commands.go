package tui

// tea.Cmd factories live here. Phase 0 has none — Phase 1 will add
// playCmd, pauseCmd, setVolumeCmd, and waitForEvent (the long-lived
// subscription that bridges audio.Player events into the Update loop).
